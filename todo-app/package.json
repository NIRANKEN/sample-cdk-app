{
  "name": "todo-app",
  "version": "0.1.0",
  "bin": {
    "todo-app": "bin/todo-app.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "22.7.9",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "aws-cdk": "2.1019.2",
    "ts-node": "^10.9.2",
    "typescript": "~5.6.3"
  },
  "dependencies": {
    "aws-cdk-lib": "2.201.0", // Contains aws-s3, aws-cloudfront, aws-cognito, aws-dynamodb, aws-lambda, aws-apigatewayv2, aws-iam, aws-amplify (for CfnApp L1)
    "constructs": "^10.0.0"
    // For more advanced Amplify L2 constructs for Next.js SSR (like deploying from local assets directly via CDK),
    // you might need to install and use 'aws-cdk-lib/aws-amplify-alpha' (for CDK v2)
    // or '@aws-cdk/aws-amplify-alpha' (for CDK v1, if applicable).
    // Example: "aws-cdk-lib/aws-amplify-alpha": "^2.201.0-alpha.0" (ensure version compatibility)
  }
}
